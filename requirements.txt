# Version numbers pinned to avoid the sklearn/xgboost/shap version-mismatch
# warnings seen during development (model was trained with scikit-learn 1.9.0,
# xgboost 3.2.0/3.3.0, shap 0.49.1-0.52.0). If you retrain the model with
# different versions, update these to match your environment:
#     pip freeze | findstr "scikit-learn xgboost shap flask scipy numpy pandas requests beautifulsoup4"
# (use `grep -i` instead of `findstr` on Mac/Linux)

flask==3.1.1
xgboost==3.2.0
shap==0.49.1
scikit-learn==1.9.0
scipy==1.18.0
numpy==2.3.1
pandas==2.3.0
requests==2.34.2
beautifulsoup4==4.15.0
